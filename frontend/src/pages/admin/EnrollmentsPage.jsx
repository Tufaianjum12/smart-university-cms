import {useEffect,useState} from 'react';import {cms} from '../../services/cmsService';import {courseApi} from '../../services/courseService';export default function EnrollmentsPage(){const [rows,setRows]=useState([]),[students,setStudents]=useState([]),[offerings,setOfferings]=useState([]),[form,setForm]=useState({student_id:'',course_offering_id:''}),[error,setError]=useState('');async function load(){try{const [a,b,c]=await Promise.all([courseApi.enrollments(),cms.list('students'),courseApi.offerings()]);setRows(a.data);setStudents(b.data);setOfferings(c.data)}catch(e){setError(e.response?.data?.detail||'Could not load enrollments')}}useEffect(()=>{load()},[]);async function save(e){e.preventDefault();try{await courseApi.enroll(form);setForm({student_id:'',course_offering_id:''});load()}catch(e){setError(e.response?.data?.detail||'Enrollment failed')}}return <><h1 className="h3">Enrollments</h1><p className="text-muted">Student ↔ Course Offering academic records.</p>{error&&<div className="alert alert-danger">{error}</div>}<form className="card card-body shadow-sm row g-3 mb-4" onSubmit={save}><div className="col-md-5"><label className="form-label">Student</label><select className="form-select" value={form.student_id} onChange={e=>setForm({...form,student_id:e.target.value})} required><option value="">Select student</option>{students.map(s=><option key={s.id} value={s.id}>{s.student_number}</option>)}</select></div><div className="col-md-5"><label className="form-label">Offering</label><select className="form-select" value={form.course_offering_id} onChange={e=>setForm({...form,course_offering_id:e.target.value})} required><option value="">Select offering</option>{offerings.map(o=><option key={o.id} value={o.id}>{o.id}</option>)}</select></div><div className="col-md-2 d-flex align-items-end"><button className="btn btn-dark w-100">Enroll</button></div></form><div className="card shadow-sm"><div className="table-responsive"><table className="table mb-0"><thead><tr><th>Student</th><th>Offering</th><th>Status</th><th>Enrolled</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{students.find(s=>s.id===r.student_id)?.student_number||r.student_id}</td><td>{r.course_offering_id}</td><td>{r.status}</td><td>{new Date(r.enrolled_at).toLocaleString()}</td></tr>)}{!rows.length&&<tr><td colSpan="4" className="text-center py-4 text-muted">No enrollments found.</td></tr>}</tbody></table></div></div></>}
